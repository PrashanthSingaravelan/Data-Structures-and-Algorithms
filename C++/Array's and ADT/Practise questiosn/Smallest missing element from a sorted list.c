#include<stdio.h>
#include<stdlib.h>

struct Array
{
	int A[10];
	int size;   // Size of an array
	int length;	// Number of elements in an array
};

void SmallestMissingvalues(struct Array obj)
  {
  	int b=0;
  	for(i=obj.A[i];i<A[-1];i++)
	{ if(i!=A[i]) { if(b<A[i]) { b=A[i]; } } }
  }

int main()
{
	struct Array obj={{1,3,4,6,7,8,9,10},8,10};
	int i;
	
	
	
}
